# Copy to .env and adjust. Defaults run zero-config on SQLite.

# SQLite (default):
DATABASE_URL=sqlite:///./poolbet.db
# Postgres (production):
# DATABASE_URL=postgresql+psycopg://poolbet:secret@localhost:5432/poolbet

DEFAULT_DISPUTE_WINDOW_HOURS=12
DEFAULT_STARTING_CREDITS=1000
DEFAULT_RAKE=0
